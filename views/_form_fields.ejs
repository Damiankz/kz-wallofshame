<div class="form-group">
  <select id="author" name="author">
    <% var options = persons; for ( var i = 0; i < persons.length; i++ ) { %>
    <option value="<%=persons[ i ].name %>" <%="selected" %>
      ><%=persons[i].name %>
    </option>
    <% } %>
  </select>
</div>
<div class="form-group">
  <textarea type="text" name="quote" id="quote"></textarea>
</div>
<a class="btn btn-white"href="/">Cancel</a>
<button type="submit" class="btn">Blame</button>

